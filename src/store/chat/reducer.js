
import * as chat from "./action-type";

let defaultState = {
    chatList: []
}

export const chatList = (state = defaultState, actions = {}) => {
    switch (actions.type) {
        case chat.type:
            return ""
    }
}